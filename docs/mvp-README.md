# MVP docs pointer (HQ)

Canonical MVP 1 documentation lives at:

**`d:\15. SookLabs\docs\mvp\`**

Key HQ docs:
- `MVP1_ACCEPTANCE.md` — eng checklist + founder walk
- `HQ_CLICK_AND_PLAY.md` — zero-OAuth click → draft sandboxes

HQ is the founder command centre. Authority is a nested Projects → SEOS panel, not the definition of HQ.

Required for the optional SEOS Authority summary panel:
- `NEXT_PUBLIC_SEOS_URL` or `SEOS_INTERNAL_URL`
- `SEOS_HQ_API_TOKEN` (same value as SEOS)
