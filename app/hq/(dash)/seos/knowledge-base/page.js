import { OpenInSeosStub } from "@/components/hq/OpenInSeosStub";

export default function HqKnowledgeBasePage() {
  return (
    <OpenInSeosStub
      title="Knowledge Base"
      blurb="Canonical business truth lives in SEOS Knowledge Base / Workspace Context. HQ does not fork FAQs or services."
    />
  );
}
