import { OpenInSeosStub } from "@/components/hq/OpenInSeosStub";

export default function Page() {
  return (
    <OpenInSeosStub
      title="Content gaps"
      blurb="Content gap analysis runs in SEOS. Use Open SEOS — HQ only tracks founder progress boards."
    />
  );
}
