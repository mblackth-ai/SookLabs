import { OpenInSeosStub } from "@/components/hq/OpenInSeosStub";

export default function Page() {
  return (
    <OpenInSeosStub
      title="Distribution map"
      blurb="Channel / surface inventory for expansion is operated in SEOS. RoastMyOpSec may consume later — Future."
    />
  );
}
