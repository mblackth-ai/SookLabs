import { OpenInSeosStub } from "@/components/hq/OpenInSeosStub";

export default function Page() {
  return (
    <OpenInSeosStub
      title="Semantic readiness"
      blurb="llms.txt / Schema.org readiness is operated in SEOS. No parallel scores in HQ."
    />
  );
}
