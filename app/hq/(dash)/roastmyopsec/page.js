import { ComingSoon } from "@/components/hq/ComingSoon";

export default function RoastMyOpSecPage() {
  return (
    <ComingSoon
      title="RoastMyOpSec"
      subtitle="Security exposure audits — may read website map and public surface from Knowledge Base, never owns business truth."
    />
  );
}
