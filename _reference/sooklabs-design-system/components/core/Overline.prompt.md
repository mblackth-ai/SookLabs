The signature mono uppercase eyebrow that opens sections and sits above headings — the control-room label motif.

```jsx
<Overline dot tone="accent">Operating rule</Overline>
<Overline>Current projects</Overline>
```

`dot` adds a glowing cyan tick. Keep the text short and spaced — 1–3 words. Sentence content, uppercased by the component.
